{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start portfolio server",
            "type": "shell",
            "command": "python -m http.server 8080",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true,
            "problemMatcher": []
        }
    ]
}
